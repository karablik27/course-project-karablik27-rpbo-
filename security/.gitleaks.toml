title = "Gitleaks Config"

[allowlist]
description = "Allowlist for false positives"
paths = ["EVIDENCE/", ".github/"]

regexes = [
  "(?i)(com\\.example\\.|app\\.example\\.)",
  "(TEST_TOKEN|DUMMY_KEY|EXAMPLE_SECRET|API_TOKEN)",
  "(?i)dummy-never-match"
]
