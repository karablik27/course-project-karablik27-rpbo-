# P10 — C1. SAST Findings & Action Plan

## Findings (Semgrep + Qodana)

### 1) Root endpoint without authentication — verify if intended
**ID:** `security.semgrep.fastapi-open-endpoint`
**Severity:**  Medium
**File:** `app/main.py`
**Line:** `@app.get("/")`

**Анализ:**
- Эндпоинт `/` открыт и доступен без аутентификации.
- Используется как health-check для Render и для проверки живости сервиса.
- Поведение намеренное и безопасное: эндпоинт не отдаёт чувствительные данные.

**Решение:**
- Эндпоинт остаётся публичным.
- Решение задокументировано в этом summary.

---

### 2) Debug print detected — remove before production
**ID:** `security.semgrep.no-print-debug`
**Severity:**  Medium
**File:** `app/main.py`
**Line:** `print("[CI] Seeded Objective(id=1)")`

**Анализ:**
- `print()` использовался для отладки при сидировании БД.
- В продакшене предпочтительно использовать структурированный логгер.

**Как решил:**
- `print()` заменён на логгер:
  ```python
  logging.getLogger("access").info("[CI] Seeded Objective(id=1)")
